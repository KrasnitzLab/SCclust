context("preprocess works properly")

#test_that("preprocess_annot returns numeric chromosomes", {
#  bin.chrom = c("chr21", "chrX", "chrY", "chr2", "chr2")
#  val = c(1,2,3,4,5)
#  df = data.frame(bin.chrom, val, stringsAsFactors=FALSE)
#  res = preprocess_annot(df)
#
#  expect_that(res$bin.chrom, equals(c(21,23,24,2,2)))
#})
